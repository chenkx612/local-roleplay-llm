# 角色扮演业务后训练学习项目

## 项目定位

本项目是一个学习与实践项目：以单个角色为业务案例，用尽可能少的数据、配置和工程复杂度，
快速体验一次完整的业务后训练流程，理解各阶段的目标、输入、输出、关键决策和常见问题，
并形成可复盘的实验记录。

项目同时强调两个性质：

- **简洁快速**：首次运行采用小数据、单配置和最低必要检查，优先缩短从准备到结果的时间。
- **链路完整**：保留业务目标、数据、Base、SFT、GRPO、统一评测和复盘，不因追求速度跳过核心阶段。

取舍原则是“缩小每个阶段，不删除任何核心阶段”。本项目不建设生产级训练平台、通用训练
流水线或规模化数据与模型基础设施。

## 学习目标

本项目按以下顺序开展实践：

```text
业务目标 + 评测口径
→ persona + style examples + 数据切分
→ Base 基线
→ Student-baseline-guided Teacher-corrected SFT 数据
→ LoRA SFT
→ GRPO
→ Base/SFT/GRPO 统一评测
→ 结果复盘
```

- 基础模型：`Qwen/Qwen3.5-2B`，训练时由 ms-swift 支持的后端以 4-bit 加载
- Student 本地推理：`mlx-community/Qwen3.5-2B-4bit`，revision`674aaa7240b91e8012fcad5d791b7dfe5ba90207`
- 训练框架：`ms-swift`
- 训练与推理：`enable_thinking=false`

在完整实践过程中验证的核心问题：

> SFT 和 GRPO 能否同时提升小模型的角色一致性、格式一致性和对话质量，并且不以牺牲其中一个目标来换取另一个目标的提升？

本项目有三个并重的目标，不设优先级：

1. **角色一致性**：身份、性格、关系、事实和边界符合 persona；语气、句式、节奏、用词和互动方式接近风格示例。
2. **格式一致性**：回复遵循项目通用格式契约：先用括号描述动作/神态，再输出口语对白。
3. **对话质量**：回答相关、自然、连贯、有信息量，并能承接后续对话。

三项目标分别评分，并以等权平均作为主要对比指标；分析时同时报告各单项，避免平均分掩盖
某一目标的明显下降。

## 实践原则

1. **简洁快速**：先用最小可行规模和一组主配置完成闭环，再决定是否扩充数据或调参。
2. **链路完整**：业务定义、数据、Base、SFT、GRPO、评测和复盘均需实际执行并留下最小产物。
3. **简化但不省略**：减少样本量、配置数、人工抽查和治理要求，不用删除阶段来换取速度。
4. **保留最低必要严谨性**：隔离训练与评测数据、固定随机种子、保存配置和结果，但不建设复杂的数据治理与恢复系统。
5. **允许负向结果**：模型没有提升也是有效结果，重点是能够解释现象、发现问题并完成复盘。

## 最小完整闭环

首次实践只要求每个核心阶段产出一份可检查结果：

| 阶段 | 最小产物 |
|---|---|
| 业务定义 | Persona、风格示例、格式契约和三目标评测口径 |
| 数据 | SFT、GRPO、Dev、Eval 四个隔离 split |
| Base | Dev/Eval 基线输出 |
| SFT | 一次有效训练、LoRA 和 Dev 输出 |
| GRPO | 一次有效训练、LoRA、奖励曲线和样本 |
| 统一评测 | Base/SFT/GRPO 在同一 Eval 上的指标与人工观察 |
| 复盘 | 结果、问题、限制和下一步记录 |

完成上述闭环即达到首次学习目标；扩大数据、调参、完整审计和第二角色复现均为可选扩展。

# 阶段一：数据与基线

**状态：未开始（重做）。** 现有 `data/` 产物仅作历史参考。为完整体验数据阶段，本次重新
准备输入和数据，并将产物写入 `data/runs/<run-id>/`。输入修改后记录新的运行标识和变更
说明即可，不引入复杂的版本治理。

## 1.1 输入

`persona.json` 必须包含 `name`、`identity`、`personality`、`speech_style`、
`relationships`、`facts`、`boundaries`，可选 `notes`。Persona 是角色事实的唯一来源，
内容应明确、无冲突。

除 `name` 外，各字段均填写为字符串数组；每一项只表达一条清晰设定。可按下表填写：

| 字段 | 含义与填写建议 |
|---|---|
| `name` | 角色名称，非空字符串 |
| `identity` | 角色是谁，以及稳定的身份、职业或所处背景 |
| `personality` | 稳定的性格、价值取向和行为倾向 |
| `speech_style` | 语言表达特征，如语气、常用句式、措辞、节奏和称呼方式 |
| `relationships` | 角色与用户或关键人物的既定关系、称呼和相处边界 |
| `facts` | 可以据此回答的确定事实，如经历、日程、物品、地点或已发生事件 |
| `boundaries` | 不应违背的设定、不可编造的范围或互动限制 |
| `notes` | 可选的补充约束或容易遗漏的设定；没有内容可省略，不要用它重复其他字段 |

`style_examples.jsonl` 准备 10～20 组代表性对话，只定义表达风格。示例应覆盖不同场景，
符合 persona，不引入额外事实，并避免重复口头禅、动作或固定暧昧模板。

当前格式契约：

```text
（简短的动作、神态或当下反应）口语对白
```

- 全角括号必须闭合，动作简短且符合语境，随后进入自然对白。
- 禁止长篇旁白、额外标签和多层括号。

输入需逐条人工审阅并通过结构校验，保存输入快照和最终使用的 system prompt。哈希校验
可作为扩展实践，不作为进入训练的前置条件。

## 1.2 数据规格与切分

数据覆盖日常对话、背景与关系、情绪与选择、格式与风格、冲突与未知事实五类场景。生成
共享候选池后按固定种子切分 SFT、GRPO、Dev 和 Eval，并检查结构、数量和精确重复。

首次实践采用较小规模，目标是控制成本并快速走通流程；完成后可按资源情况扩展：

| 数据 | 首次实践 | 可选扩展 |
|---|---:|---:|
| Pilot Prompt | 5 | 10 |
| SFT Train Prompt / 样本 | 50 | 100 |
| GRPO Prompt | 20 | 30 |
| Dev Prompt | 10 | 20 |
| Eval Prompt | 20 | 50 |

SFT、Dev、GRPO、Eval 分别用于监督训练、过程观察、强化学习和最终评测；Dev、GRPO、
Eval 不进入 SFT。首次切分后保存随机种子和各 split 文件。学习阶段只要求避免明确的数据
泄漏，不要求全量近似重复审计。

## 1.3 Pilot 与 Teacher-corrected SFT

核心链路：

```text
persona + train prompt → Student baseline
persona + style examples + train prompt + baseline → Teacher 最小纠错
```

这是 Student baseline 条件化的 Teacher 纠错，不称为 Student-aware SFT。

Teacher 要求：

- 分别检查角色一致性、格式一致性和对话质量。
- 合格回答原样保留，不合格回答做最小充分修改。
- 不补充 persona 和用户消息之外的事实；未知信息应明确保留为未知。

正式生成前先运行 5 条 Pilot，五类场景各 1 条。人工检查 persona、风格、格式和 Teacher
改写质量；如发现系统性问题，修改输入或 rubric 后重新运行。Pilot 只用于验证链路，不进入
Dev 或 Eval。

生成失败时进行有限重试并保存进度。完成后自动检查结构、格式、空回答、截断和明显复读，
再人工抽查 5～10 条样本。只修复会阻碍训练或明显污染数据的问题，其他不足记录到复盘中。

## 1.4 Base 基线与阶段检查

训练前生成 Dev 和 Eval 的 Base 输出。Dev 用于阶段观察，Eval 只用于最终对比，不根据
Eval 结果修改数据或训练配置。Base、SFT、GRPO 尽量使用相同的 checkpoint、persona
prompt、chat template 和生成参数；差异必须记录。

满足以下最低条件后进入阶段二：

- Persona、风格示例和 system prompt 可以正常加载。
- SFT、GRPO、Dev、Eval 数量正确且没有精确重复泄漏。
- SFT 样本可以被训练脚本读取，抽查未发现阻断训练的系统性问题。
- Dev/Eval Base 输出已生成，可作为后续对比基线。
- 保存数据规模、随机种子、生成配置和已知问题的简要记录。

阶段一不要求输入哈希、多轮 Reviewer、全量语义审计、高相似度自动审计或生产级恢复。

# 阶段二：LoRA SFT

## 2.1 目标

- 实践一次 QLoRA 数据准备、训练、保存、加载和推理流程。
- 提升角色一致性：遵循身份、性格、关系、事实和边界，并学习角色的语气、句式、节奏、
  用词和互动方式，而不是机械插入口头禅。
- 提升格式一致性：稳定学习“括号动作/神态 + 口语对白”的回复格式。
- 提升对话质量：保持回答自然、有针对性，减少复读、冗长动作描写和模板化回复。
- 保留已有对话和指令遵循能力。

## 2.2 配置

```yaml
model: Qwen/Qwen3.5-2B
train_type: lora
dtype: bf16
quant_method: bnb
quant_bits: 4
max_length: 1024
lora_rank: 16
lora_alpha: 32
lora_dropout: 0.05
learning_rate: 5e-5
num_train_epochs: 1
batch_size: 1
gradient_accumulation_steps: 16
enable_thinking: false
```

只对 assistant 回复计算 loss。不要把 MLX 转换权重直接交给 ms-swift；训练使用
官方 Transformers checkpoint，并由 ms-swift/BNB 进行 4-bit QLoRA 加载。

首次实践只运行上述默认配置：`learning_rate=5e-5`、`num_train_epochs=1`。完成全部流程后，
如希望观察超参数影响，只选择一个变量做一组对照：

- 学习率：`2e-5` 或 `1e-4`
- Epoch：`2`

对照实验使用 Dev 结果比较，不要求搜索最优配置，也不要求合并 LoRA。

## 2.3 阶段观察与继续条件

比较 `Base + Persona` 与 `SFT + Persona`：

- 训练是否正常结束，loss 是否可解释，LoRA 是否可以重新加载并生成回答。
- 角色一致性、格式一致性和对话质量相对 Base 如何变化。
- 是否出现复读、模板化、统一拒答、回复过短或格式投机。
- 数据、配置和训练日志中哪些因素可能解释观察到的变化。

如果出现训练中断、NaN、适配器无法加载或输出为空等技术失败，先修复后再继续。如果训练
和推理链路有效，即使 SFT 未优于 Base，也进入 GRPO，并将该结果作为后续分析背景。

产物：

```text
outputs/sft_adapter/
outputs/sft_eval.json
```

# 阶段三：GRPO

## 3.1 目标与训练

本阶段重点体验组内采样、奖励计算、策略更新和奖励投机观察。每个 Prompt 采样 4 个回答，
按组内相对奖励优化，并从阶段二得到的 SFT LoRA 继续训练。

```yaml
rl_prompts: 20
num_generations: 4
max_completion_length: 256
learning_rate: 1e-6
epochs: 1
enable_thinking: false
```

首次实践只运行一组配置，不进行奖励权重或训练参数搜索。如果 SFT 效果不理想但模型仍能
正常生成，则继续训练并在最终报告中分析它对 GRPO 起点和结果的影响。

## 3.2 奖励

```text
R = (RoleConsistency + FormatConsistency + DialogueQuality) / 3 - Penalty
```

| 维度 | 内容 |
|---|---|
| RoleConsistency | 身份、性格、关系、事实、边界，以及语言风格的整体模仿质量 |
| FormatConsistency | 开头括号动作、括号闭合、动作简短且贴合语境、随后进入对白 |
| DialogueQuality | 相关、自然、连贯、有信息量、可继续对话 |

本地规则检查可确定的格式条件，Teacher/Judge 对三个主目标分别给出 0～10 分。语言风格
作为 `RoleConsistency` 的子维度评分，不再单独取得第四份权重。`Penalty` 只处理重复、
乱码、照抄示例等三个主分容易漏掉的奖励投机，不重复惩罚已经体现在主评分中的普通缺陷：

| 问题 | 惩罚 |
|---|---:|
| 复读或乱码 | -3 |
| 大段复述 persona | -2 |
| 照抄风格示例或堆叠口头禅 | -3 |
| 用空洞、重复的括号动作骗取格式分 | -2 |

首次实践优先保证奖励函数可以稳定计算，并人工检查至少 5 组候选及其分数。训练中记录
平均奖励、回答长度和复读率，用于观察策略变化和奖励投机，不根据 Eval 结果反复调奖励。

产物：

```text
outputs/grpo_adapter/
outputs/reward_curve.json
outputs/grpo_samples.jsonl
```

# 阶段四：评测

## 4.1 对比

```text
A. Base + Persona
B. SFT LoRA + Persona
C. GRPO LoRA + Persona
```

三组使用相同 Eval、生成参数和 Judge。

## 4.2 指标

三个并列主指标：

- Role Consistency
  - Identity/Facts/Boundaries
  - Personality/Relationships
  - Style Fidelity（语气、句式、节奏、用词和互动方式）
- Format Consistency
  - Format Contract Pass Rate（确定性规则）
  - Format Quality（动作简洁度、语境贴合度及对白衔接）
- Dialogue Quality
  - 相关性、自然度、连贯性、信息量和可继续对话性

诊断指标：

- 人设矛盾率
- 未知事实编造率
- 出戏率
- 复读率、模板化率和回答长度分布

自动风格评测结合与 `style_examples.jsonl` 对齐的 Judge rubric；词频、固定短语命中和长度
统计只用于诊断，不能代替 Style Fidelity。首次实践人工检查 10 条 Eval，条件允许时采用
Base/SFT/GRPO 匿名两两比较，分别询问“哪个更像这个角色”“哪个格式更自然稳定”和
“哪个回答本身更好”。

准备 5～10 条不使用 persona 的小型能力保持集，覆盖普通问答、总结、改写、基础推理和
指令遵循。该集合用于观察明显回归，不作为通用能力的严格结论。

时间允许时可将 Eval 和人工比较扩展到 30～50 条，以降低小样本波动。

## 4.3 实验判断与学习完成标准

以下指标用于判断训练是否带来预期收益，不作为学习项目必须得到正向结果的前提。即使
SFT 或 GRPO 未达到预期，只要完整记录现象、定位原因并形成复盘，仍属于有效的实践结果。

实验分析问题：

- SFT 相对 Base 在三个主指标上分别发生了什么变化，哪些变化可能来自数据或训练配置？
- GRPO 相对 SFT 是否提高了奖励对应的行为，是否同时产生奖励投机或其他退化？
- 自动评分、格式规则和人工观察是否一致；不一致时哪一种更可信，原因是什么？
- 角色能力提升是否伴随明显的通用能力、回答长度或多样性变化？
- 当前样本规模和评测设计支持哪些结论，哪些结论仍需要更多数据验证？

学习完成标准：

- 至少用一个角色完成从业务目标与数据定义到 SFT、GRPO、统一评测和结果复盘的全部阶段。
- SFT 和 GRPO 均完成一次有效训练，产物可加载；技术失败需修复或清楚记录原因与替代实践。
- Base、SFT、GRPO 使用同一 Eval 完成对比，并保留自动指标和人工样本观察。
- 各阶段保留必要的输入、配置、日志、产物和决策依据，能够说明结果、限制和主要问题。

# 实施顺序

## Milestone 1：实践数据

**状态：未开始（重做）。**

- [ ] 重写 persona、style examples 和格式契约，完成 5 条 Pilot。
- [ ] 生成首次实践规模的共享候选池并切分、去重。
- [ ] 生成 Teacher-corrected SFT，完成自动检查和人工复核。
- [ ] 生成 Dev/Eval Base；记录检查结果后进入阶段二。

## Milestone 2：SFT 实践

- [ ] 用一组默认配置完成 QLoRA SFT。
- [ ] 加载 LoRA，在 Dev 上生成回答并与 Base 做初步比较。
- [ ] 记录训练曲线、输出样本、技术问题和阶段观察。

## Milestone 3：GRPO 与统一评测

- [ ] 检查奖励函数在至少 5 组候选上的行为。
- [ ] 用一组默认配置完成 GRPO，并记录奖励、长度和复读率变化。
- [ ] 在同一 Eval 上生成 Base、SFT 和 GRPO 输出。
- [ ] 完成自动指标、10 条人工检查和小型能力保持集观察。

## Milestone 4：复盘与沉淀

- [ ] 汇总各阶段的输入、配置、产物、评测结果和关键决策。
- [ ] 输出 LoRA、评测报告、运行说明和学习复盘。
- [ ] 列出失败点、结果限制、下一轮最值得验证的一个改进方向。

可选扩展包括扩大数据与 Eval、运行一个单变量对照实验，或用第二个角色再次实践；这些内容
不阻塞首次学习闭环完成。

# 核心实验

| 模型 | 角色一致性（含风格） | 格式一致性 | 对话质量 | 三目标宏平均 | 能力保持 |
|---|---:|---:|---:|---:|---:|
| Base + Persona | 待测 | 待测 | 待测 | 待测 | 待测 |
| SFT | 待测 | 待测 | 待测 | 待测 | 待测 |
| SFT + GRPO | 待测 | 待测 | 待测 | 待测 | 待测 |

待验证假设：

- Teacher-corrected SFT 可能优先改善格式一致性和已被 Teacher 修正过的典型角色缺陷。
- GRPO 可能强化奖励函数覆盖的行为，也可能放大奖励漏洞、模板化或回答长度偏差。
- 小规模数据和单次训练的结论可能波动较大，应将结果视为学习案例而非普遍结论。
