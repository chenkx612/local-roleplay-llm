# 阶段一 Review Issues

## 1. [高] 数据生成可能少于目标条数，但仍报告成功并覆盖产物

位置：`src/roleplay/datagen.py:238-269`、`src/roleplay/datagen.py:358-380`

`_generate_for_scenario()` 按预先计算好的固定批次数循环。Teacher 某一批只返回部分
合法记录时（例如请求 5 条但只返回 1 条），代码仍然把 `offset` 增加 5，且不会补生成
缺少的 4 条。所有批次结束后，`generate()` 会照常写文件、打印“生成完成”并成功返回。

现有 `test_retry_on_empty_response` 已经能复现这一点：该测试运行时最终生成了 96 条 SFT，
而 smoke profile 要求 100 条，但测试没有断言数量，程序也没有失败。

这会让后续训练在不知情的情况下使用残缺数据，并可能用残缺结果覆盖上一次完整产物。
生成逻辑应持续补齐每个场景的目标数量；达到最大重试次数后如果仍不足，应以非零状态失败，
并避免替换已有产物。测试还应覆盖“部分有效回复”和“重试后仍不足”两种情况。

## 2. [高] 当前 Base 基线输出大量截断和退化，不能作为有效对照

位置：`src/roleplay/inference.py:17-35`、`data/baseline_outputs.jsonl`

推理固定使用 `max_tokens=256`，但没有读取或保存 API 返回的 `finish_reason`。当前保存的
50 条结果中，多条明显在句中结束，例如第 3 条以“我的”结尾；另有大量异常循环，例如
“蛋蛋蛋蛋……”“特别特别……”“她她她她……”和“肉色肉色……”。

这些结果并不是完整、稳定的 Base 回答。如果阶段二继续用它判断 SFT 是否优于 Base，
结论会主要反映基线截断或采样退化，而不是微调效果。

基线生成应检测 `finish_reason`，把 token 上限导致的截断视为失败或自动续跑；同时应对明显
复读做失败标记并重新检查生成参数。修复后需要重新生成整份
`data/baseline_outputs.jsonl`，不能继续使用当前文件。

## 3. [高] GRPO 训练集与“独立”评测集存在精确泄漏

位置：`data/rl_train.jsonl`、`data/eval.jsonl`

问题“你以前做过类似的工作吗？”同时出现在 `rl_train.jsonl` 和 `eval.jsonl`。虽然三份
数据是分别请求 Teacher 生成的，但代码没有在写出前做跨数据集精确去重，因此不能保证
PLAN 中要求的独立评测。

这会使 GRPO 在训练期间直接见到评测问题，污染 Base/SFT/GRPO 对比。生成完成后至少应对
规范化后的 Prompt 做跨 split 精确去重并补齐缺失条目；当前 RL 和 eval 数据也需要重新
拆分或重新生成。

## 4. [中] Base 默认使用 4-bit 社区模型，与计划中的训练基座不一致

位置：`src/roleplay/inference.py:15`、`PLAN.md` 的“基础模型使用”和阶段二技术方案

基线默认模型是 `mlx-community/Qwen3.5-2B-4bit`，而 PLAN 指定
`Qwen/Qwen3.5-2B`，阶段二还明确优先使用 BF16 LoRA。若 SFT/GRPO 基于官方 BF16 权重，
Base 却使用另一份 4-bit 转换权重，量化与转换差异会成为额外变量，三阶段不再是同一基座
上的公平比较。

应让 Base、SFT 和 GRPO 使用同一基础 checkpoint、同一精度/量化策略和同一推理后端；
至少要把实际 checkpoint、revision、dtype/量化方式和生成参数记录进输出元数据。

## 5. [中] 单条推理失败会被吞掉，命令仍声称整批完成

位置：`src/roleplay/inference.py:63-80`

任意 API 异常都会被 `except Exception` 捕获并转换成空字符串，随后写入输出。即使 50 条
全部请求失败，命令仍会打印“完成，共 50 条”并以状态码 0 退出。后续评测很容易把空回答
当成真实 Base 表现，而流水线无法发现基线实际没有生成成功。

应记录失败数，并在存在失败时重试；重试后仍失败则让命令以非零状态退出，或至少将整批
标记为不完整，不能打印无条件成功。也应校验最终记录数、非空回答数与 eval 输入数完全一致。
