# Legacy data artifacts

These files were generated before the `data/runs/<run-id>/` workflow was
introduced. They belong to the previous persona and are retained only for
historical reference. Do not use them as inputs or evaluation data for the
current run.

