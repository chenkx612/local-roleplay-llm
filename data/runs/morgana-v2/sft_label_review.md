# 正式 SFT 标签复核

- 复核日期：2026-08-08
- Teacher prompt：v8
- 自动检查：结构、输入哈希、逐条对齐、非空、乱码和明显复读均通过
- 人工结论：**通过学习项目门槛，可用于 SFT**

## 2026-08-09 第三次训练后定向清洗

- 根据第三次 SFT 的 Dev 表现复查训练标签，仅确认 `sft_0003` 和 `sft_0029` 需要修复；
  `sft_0031` 的不确定推测属于自然对话，`sft_0049` 对其他猫语言的判断允许合理创作，均保留。
- `sft_0003` 删除过长动作、重复情绪、无依据能力和威胁表达，改为简短的嘴硬后承认困倦并接受休息。
- `sft_0029` 删除“拒绝学狗叫”后又以学不像为耻的逻辑冲突，以及低姿态的赔罪表达，改为明确拒绝激将并维护伙伴身份。
- `sft_teacher_edits.jsonl` 保留 Teacher v8 原始输出，不回写人工裁决；当前训练标签以
  `sft_train.jsonl` 为准。
- 清洗后仍为 50 条，50/50 含“吾辈”、0/50 含“本大爷/本喵”，平均 125.2 字符，最长
  246 字符；SHA-256 为
  `2c05d7618f433d3ddf972c8563ae8c3e5662c9ab227fbe83fe01f0282c4f720d`。

## 汇总

- 冻结的 50 条 Student baseline 未改变，SHA-256 为
  `8e69b5de4012e55e76692e4f597c69499f351c4499b28c9176faa0264c564108`。
- Teacher 决策：50 `rewrite`、0 `light_rewrite`、0 `keep`。
- 最终标签：0 空回答、0 乱码、0 明显复读，平均 136.8 字符。
- v8 已修复原有主客体、出戏、自称和严重复读问题，没有发现阻断训练的严重标签。

## 非阻断观察

| ID | 问题 |
|---|---|
| `sft_0031` | 先承认不知道再猜测名字含义，符合自然对话情景，保留。 |
| `sft_0049` | 能否理解其他猫均允许作为不冲突的合理创作，保留。 |

## 已验证改进

- `sft_0036`：删除不自然的“给莲一条鱼”奖励，改为明确、可执行的支持。
- `sft_0043`：不再承认作品或片场身份，保持角色内视角。
- `sft_0029`：Teacher v8 已识别“小猫”和错误自称；训练后人工清洗进一步修复内部逻辑和角色姿态。

## 阶段决策

本项目以小规模完整跑通后训练为目标。当前标签已满足非空、完整、可读、角色身份不崩坏、
能回应用户的最小门槛，可进入 SFT。上述局部瑕疵仅作为复盘材料。
