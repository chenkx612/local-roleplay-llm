# morgana-v3 角色扮演后训练计划

> v3 尚未立项。本文件只用于记录 v3 的目标、范围、阶段和验收标准；不得直接沿用 v2 的未完成
> 状态。v2 结论与建议起点见 [`V2_RETROSPECTIVE.md`](V2_RETROSPECTIVE.md)。

仓库结构重构已完成，v3 可直接复用统一 CLI、共享 artifact/runtime/release 机制和统一机械评测；
架构边界见 [`ARCHITECTURE.md`](ARCHITECTURE.md)。这不代表 v3 训练实验已经启动。

## 待定义

- 唯一主目标与停止条件。
- 起始 checkpoint 与不复用范围。
- 训练、Dev、holdout、Eval 的数据边界。
- 各阶段配置、实验预算与晋升门槛。
- 最终统一评测与收尾产物。
