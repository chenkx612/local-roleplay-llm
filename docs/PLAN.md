# morgana-v2 角色扮演后训练计划

## 项目目标

用一个角色、一个小模型和最小规模的数据，完整走通一次角色扮演模型后训练闭环：

```text
业务目标与 Persona
→ 数据准备与 Base 基线
→ SFT
→ GRPO
→ Base / SFT / GRPO 统一评测
→ 复盘
```

本轮只回答一个问题：

> SFT 和 GRPO 能否依次让 2B 小模型稳定生成、保持角色，并产出有意义的对话？

项目优先保证两件事：

- **流程完整**：核心阶段不能省略，每一阶段都要留下可检查的输入、输出和结论。
- **简单快速**：每阶段只采用一组主配置和最小样本，不做超参数搜索，不建设生产级平台。

## 判断模型是否变好

按以下三个前置关系逐层判断，不用单一总分掩盖底层失败：

1. **生成稳定性**：回答非空、完整、连贯、可读，没有乱码、严重复读或破坏性截断。
2. **角色一致性**：在稳定生成的前提下，身份、性格、关系、边界、视角和整体语言风格符合
   Persona 及当前对话。
3. **对话质量**：在角色成立的前提下，回答自然、相关、有内容，并能支持对话继续。

动作括号、emoji、固定开头、自称频率和回答长度只用于诊断，不是优化目标。无外置记忆时，
也不要求模型机械复述固定事实。

## 核心流程

### 1. 数据与 Base

冻结 Persona、风格样例和 system prompt，建立彼此隔离的 SFT、GRPO、Dev、Eval 数据。
SFT 标签由 Student 先回答、Teacher 做最小必要纠错；随后用固定推理条件建立 Base Dev 基线，
供后续 SFT 和 GRPO 对照。

这一阶段的关键是：输入可追溯、数据无泄漏、标签可检查、Base 可作为后续公平对照。

### 2. SFT

在 Base 模型上完成一次固定配置的 QLoRA SFT，只学习 assistant 回复。训练后先验证参数确实发生
有效更新，再在同一推理条件下比较 Base 与 SFT。

只有 SFT 同时通过技术有效性、生成稳定性和匿名人工复核，才进入 GRPO。行为没有改善时保留
负向结果并停止，不在同一轮静默调参。

### 3. GRPO

从通过验收的 SFT adapter 继续训练。奖励只围绕可读性、角色一致性和对话质量，并惩罚照抄
Persona 或示例等明显投机行为。

训练前人工检查奖励是否符合直觉；训练后沿用 SFT 的分层方法与 SFT 比较。失败时保留结果，
不利用 Eval 反复调整奖励。

### 4. 统一评测与复盘

使用从未参与开发决策的 Eval，在完全一致的推理条件下生成 Base、SFT、GRPO 回答，依次报告
生成稳定性、角色一致性和对话质量，并保留代表性样本。

复盘分别回答 SFT 相对 Base、GRPO 相对 SFT 发生了什么，同时说明小样本、单角色、单次训练和
Judge 偏差带来的结论边界。

## 阶段决策

```text
数据与 Base 有效？ ─否→ 修复数据或链路
        │是
        ▼
SFT 技术、稳定性、人工复核均通过？ ─否→ 记录负向结论并结束
        │是
        ▼
GRPO 技术与分层评估通过？ ─否→ 记录负向结论并结束
        │是
        ▼
统一评测 → 复盘
```

## 完成定义

当数据准备、Base、SFT、GRPO、统一评测和复盘都已执行，并各自留下可检查的最小证据时，v2
闭环完成。完成不等于模型一定提升，而是能够基于可复现、可比较的结果回答本轮核心问题。

## 相关文档

- [`V2_EXECUTION_SPEC.md`](V2_EXECUTION_SPEC.md)：数据规模、模型配置、seed、验收阈值和产物要求。
- [`RUNLOG.md`](RUNLOG.md)：实际运行配置、产物、偏差、观察和最终结论。
- [`ISSUES.md`](ISSUES.md)：已知问题与后续处理。
- [`V1_RETROSPECTIVE.md`](V1_RETROSPECTIVE.md)：v1 复盘；v1 产物不参与 v2 训练或正式评测。
