# morgana-v1 实验复盘

## 1. 版本结论

`morgana-v1` 于 2026-08-08 收尾。v1 完成了 Persona、数据切分、Teacher-corrected SFT、
Base 基线和三次 QLoRA SFT，验证了从数据到可加载 adapter、同后端 Dev 对比的主要工程链路；
没有进入 GRPO，也没有完成 Base/SFT/GRPO 统一评测。

v1 的最终价值不是证明模型已经达到可用标准，而是完成了两个学习目标：

1. 找到并修复了数据泄漏、残缺写盘、无效训练和不可比推理等会使实验结论失真的问题。
2. 发现原目标同时追求格式、事实、风格和内容，既分散训练信号，也让低价值的格式规则能够
   单独否决已有改善，因此为 v2 收敛出三个递进目标。

v1 的所有数据与模型产物保持只读，不再按新口径回写状态。v2 使用新的运行目录重新开始。

## 2. 实验范围与版本边界

- 角色：摩尔加纳。
- 数据目录：`data/runs/morgana-v1/`。
- SFT 目录：`output/morgana-v1/stage2-sft/1/`、`2/`、`3/`。
- Student/Base 本地模型：`mlx-community/Qwen3.5-2B-4bit`，revision
  `674aaa7240b91e8012fcad5d791b7dfe5ba90207`。
- 训练基座：`Qwen/Qwen3.5-2B`，revision
  `965dcc54bc9c0591873df0e9869c056a54d323d1`。
- Teacher：`deepseek-v4-flash`，开启 thinking，`reasoning_effort=high`。
- 训练环境：Colab 2026.04、Tesla T4、ms-swift 4.4.1、4-bit BNB QLoRA。

v1 数据和 Teacher rubric 强调固定动作格式、风格、事实/背景约束、角色和对话质量。目标调整发生
在第三次 SFT 之后，因此第三次只可作为旧数据上的训练证据，不能替代 v2 实验。

## 3. 阶段一结果：数据与 Base

### 3.1 数据链路

以五类场景各 20 条构建了 100 条全局唯一 Prompt，并使用固定种子切分：

| Split | 数量 | 每类场景 | 用途 |
|---|---:|---:|---|
| SFT | 50 | 10 | Teacher 纠错与监督训练 |
| GRPO | 20 | 4 | 预留强化学习 |
| Dev | 10 | 2 | 阶段观察 |
| Eval | 20 | 4 | 预留统一评测 |

四个 split 无规范化精确重复。生成器在 v1 中补齐了全局去重、数量不足失败、有限重试、整批原子
写入和输入快照，解决了早期 GRPO/Eval 泄漏及残缺数据仍覆盖正式产物的问题。

### 3.2 Teacher-corrected SFT

- Pilot 5 条，五类场景各 1 条，5/5 通过当时的自动检查和人工复核。
- 正式数据 50 条，Student baseline、Teacher edits 和训练标签逐条对齐。
- Student baseline 中 44 条正常结束、6 条达到 512 token 上限；这些 bad case 被保留给 Teacher
  纠错。
- Teacher 决策为 48 条 `rewrite`、2 条 `light_rewrite`；人工抽检修正了 1 条对共同经历确认
  过强的回答。
- 最终 `sft_train.jsonl` 为 ms-swift 可读取的 `messages` 结构，最长样本 816 tokens。

这套数据验证了完整构建链路，但多数回答被重写，且标签受严格格式和旧 rubric 强烈影响。它不再
作为 v2 训练数据复用。

### 3.3 Base 基线

阶段一使用 MLX 4-bit Base，配置为 `max_tokens=256`、`temperature=0.6`、`top_p=0.8`、
`top_k=20`、`presence_penalty=0.4`、`repetition_penalty=1.45`，关闭 thinking。

- Dev：10/10 非空且以 `stop` 结束，全部首次成功。
- Eval：20/20 非空且以 `stop` 结束，全部首次成功。
- 输出与冻结输入逐条对齐，模型 revision、配置和输入输出哈希均已保存。

该结果证明本地部署基线可用；由于 MLX 与训练时 TransformersEngine 的惩罚和模板语义不同，
它不能作为 LoRA 因果比较的主要 Base。这个问题在后续运行中通过同后端 Base 得到修正。

## 4. 阶段二结果：三次 QLoRA SFT

| 次数 | Run ID | 主变化 | 技术结果 | Dev/行为结果 | 决策 |
|---|---|---|---|---|---|
| 1 | `20260807T170226Z-ad78fb8f` | FP16，1 epoch，4 steps | 4 个 `grad_norm` 均为 NaN；186 个 LoRA-B 全为零，训练无效 | SFT 5/10 截断，且与 MLX Base 后端不一致 | 技术失败，禁止进入 GRPO |
| 2 | `20260808T053633Z-01b40cf2` | FP32，1 epoch，4 steps | 梯度有限；186/186 LoRA-B 非零；adapter 可重载 | 同后端 Base 截断 1/10，SFT 截断 3/10；双方严格格式 0/10 | 有效训练，但旧机械门槛失败 |
| 3 | `20260808T094035Z-7918de52` | FP32，3 epochs，12 steps | 12 个梯度有限且为正；186/186 LoRA-B 非零；adapter 可重载 | 同后端三 seed 共 30 对，稳定性和部分角色诊断改善，但有 1 条不可读输出 | 有效训练，未获准进入 GRPO |

### 4.1 第一次：识别“训练跑完但参数未更新”

第一次 loss 从 `2.6794` 波动到 `2.4434`，checkpoint 也可以加载，但 LoRA-B 保持初始化零值，
说明 optimizer step 实际被非有限梯度跳过。这个结果证明 loss、checkpoint 存在和生成非空都不足以
验收训练有效性。

后续训练流程因此增加三项硬检查：`grad_norm` 必须有限且为正、全部 LoRA-B 必须出现非零
更新、adapter 必须能重新加载生成。

### 4.2 第二次：解决技术有效性，暴露行为门槛问题

第二次改为 FP32 模型计算、FP32 BNB compute 和 FP32 LoRA 参数，1 epoch 即产生有效更新。
同时增加同一 Transformers 后端的无 adapter Base，解决了主要后端混杂。

不过 SFT 的截断由 Base 的 1/10 增至 3/10，严格动作格式双方均为 0/10，旧机械门槛未通过。
因此没有开始人工复核，也没有进入 GRPO。

### 4.3 第三次：有效更新和最有价值的行为证据

第三次保持数据、模型 revision、LoRA 结构和学习率不变，只将有效 FP32 配置从 1 epoch 增至
3 epochs：

- 12 个 optimizer step，耗时 676.511 秒。
- loss 从首步 `2.6791` 波动下降至末步 `1.9329`。
- 186/186 个 LoRA-B 张量非零，adapter SHA-256 为
  `cb26676f1d9d087db3796401e12c7fe219bb6d2a3fc6bf8989edb9e7fba5eb3e`。
- 同一 Transformers 后端、三个固定 seed 生成 Base/SFT 各 30 条并完全对齐。

主要机械结果：

| 指标 | Base | SFT | 变化 |
|---|---:|---:|---:|
| `finish_reason=stop` | 24/30 | 25/30 | +1 |
| 截断 | 6/30 | 5/30 | -1 |
| v1 旧退化标记 | 24/30 | 10/30 | -14 |
| 错误自称 | 22/30 | 8/30 | -14 |
| 严格动作格式 | 0/30 | 0/30 | 0 |

旧门槛因“严格格式率至少提高 20 个百分点”失败，归档状态为 `behavior_failed`。目标收敛后进行
只读重评，完整对齐、结束数、截断和严重复读均通过，但 `20260808:dev_0010` 出现连续希腊字母
和随机字符，因此仍未通过生成稳定性。人工角色一致性和对话质量复核没有开始。

可以合理地说第三次 SFT 在多个机械指标上优于同后端 Base；不能据此声称角色一致性或对话质量
已经整体提升，也不能声称满足新目标。

## 5. v1 解决的工程问题

v1 已形成并由测试覆盖的最小可靠链路：

- 四个 split 先全局去重并完整生成，再原子发布，避免训练/评测泄漏和残缺覆盖。
- 推理保存 `finish_reason`，对 API 异常、空回答、截断和明显复读有限重试；整批失败不覆盖旧
  产物。
- 训练验收检查实际梯度和 LoRA-B 更新，不再把“命令正常结束”误认为有效训练。
- SFT 前后对比使用同一模型 revision、同一 Transformers 后端、同一 seed 和对齐输入。
- 自动评估与人工评估分层，历史产物和当时结论不被新规则静默改写。

这些实现和测试可以复用于 v2；v1 的生成数据、system prompt、Base 输出和 adapter 不复用。

## 6. 目标调整及其依据

v1 同时关注固定动作格式、风格、事实/背景约束、角色表现和内容质量。第三次实验显示，严格格式
没有改善，却能单独阻断流程；错误自称等诊断则虽明显改善，也不能替代对角色和对话本身的人工
判断。事实约束还容易压缩角色扮演所需的合理创作空间，而项目又没有外置记忆系统支持严格事实
一致性。

v2 因此只保留三个递进目标：

1. **生成稳定性**：先保证回复完整、连贯、可读，无乱码、严重复读和破坏性截断。
2. **角色一致性**：在可读基础上判断核心身份、性格、关系、边界和视角是否成立。
3. **对话质量**：最后判断回复是否相关、自然、有意义并能承接对话。

动作括号、emoji、固定开头、自称频率和长度降为诊断信息。事实可靠性不独立评分；允许不违背
核心 Persona 和当前对话的合理创作，但仍禁止擅自编造用户个人经历或重大共同关系。

## 7. 结论边界与未完成项

- v1 完成到有效 SFT，没有执行 GRPO。
- 没有在统一 Eval 上比较 Base、SFT 和 GRPO。
- 第三次 SFT 没有通过生成稳定性，也没有完成人工角色/对话复核。
- 样本很小、只有一个角色、每个主配置只运行一次，不能外推到其他角色或模型。
- v1 数据与目标耦合，不能只替换评估规则后把旧 adapter 宣称为 v2 结果。

因此 v1 以“工程链路验证成功、旧实验目标未完成、研究问题迁移到 v2”正式收尾。下一步从
`morgana-v2` 的 Persona/system prompt、数据和 Base 基线重新建立可比实验。

## 8. 归档索引

- 阶段一事实与产物：`data/runs/morgana-v1/`
- 第一次 SFT：`output/morgana-v1/stage2-sft/1/`
- 第二次 SFT：`output/morgana-v1/stage2-sft/2/`
- 第三次 SFT：`output/morgana-v1/stage2-sft/3/`
- v1 问题记录：`ISSUES.md`
